# 🔗 链接：
# 源文件：https://github.com/obytes/react-native-template-obytes/blob/master/.github/workflows/type-check.yml

# ✍️ 描述：
# 此动作用于在项目上运行类型检查。
# 在拉取请求和推送到 main/master 分支时运行
# 根据事件类型：
#   - 如果是拉取请求，它将运行类型检查，然后将检查添加到 PR 以及使用 reviewdog 注释代码中的错误。
#   - 如果是推送到 main/master，它将运行类型检查，如果有任何错误则失败。

# 🚨 需要 GITHUB 密钥：无

name: Type Check (tsc)

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read
  pull-requests: write

jobs:
  type-check:
    name: Type Check (tsc)
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout project repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Setup Node + PNPM + install deps
        uses: ./.github/actions/setup-node-pnpm-install

      - name: 📦 Install Reviewdog
        if: github.event_name == 'pull_request'
        uses: reviewdog/action-setup@v1

      - name: 🏃‍♂️ 运行 TypeScript PR # Reviewdog tsc 错误格式：%f:%l:%c - error TS%n: %m
        # 我们只需要在拉取请求时添加 reviewdog 步骤
        if: github.event_name == 'pull_request'
        run: |
          pnpm type-check | reviewdog -name="tsc" -efm="%f(%l,%c): error TS%n: %m" -reporter="github-pr-review" -filter-mode="nofilter" -fail-on-error -tee
        env:
          REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name:
          🏃‍♂️ 运行 TypeScript 提交
          # 如果不是拉取请求，那么我们只需要运行类型检查
        if: github.event_name != 'pull_request'
        run: pnpm type-check
