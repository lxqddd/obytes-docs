# 🔗 链接：
# 源文件：https://github.com/obytes/react-native-template-obytes/blob/master/.github/workflows/new-github-release.yml
# Starter 发布流程：https://starter.obytes.com/ci-cd/app-releasing-process/

# ✍️ 描述：
# 此工作流将在新应用版本工作流成功执行后自动触发。
# 它将使用新的应用版本和发布说明创建一个新的 GitHub 发布。

# 🚨 需要 GITHUB 密钥：无

name: New GitHub Release

on:
  push:
    tags:
      - '*'

jobs:
  release:
    name: New GitHub Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📦 Checkout project repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏃‍♂️Create A Draft Github Release
        uses: ncipollo/release-action@v1
        with:
          generateReleaseNotes: true
          draft: false
