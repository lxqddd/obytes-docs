# 🔗 链接：
# 源文件：https://github.com/obytes/react-native-template-obytes/blob/master/.github/workflows/e2e-android.yml
# 端到端测试：https://starter.obytes.com/testing/end-to-end-testing/

# ✍️ 描述：
# 此工作流用于使用 Maestro Cloud 在 Android 上运行端到端测试。
# 作为第一步，它将使用 Gradle 构建生成测试 APK，然后触发 Maestro Cloud 在生成的 APK 上运行测试。
# 此工作流将在带有 "android-test-maestro-cloud" 标签的拉取请求（PR）上触发，或可以从 Actions 选项卡手动触发。

# 🚨 需要 GITHUB 密钥：
# MAESTRO_CLOUD_API_KEY：Maestro Cloud 的 API 密钥。您可以从 https://cloud.mobile.dev/ci-integration/github-actions#add-your-api-key-secret 获取它

name: E2E Tests Android (Maestro Cloud)

on:
  workflow_dispatch:
  pull_request:
    branches: [main, master]

jobs:
  generate-and-test-apk:
    if: github.event_name != 'pull_request' || ( github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'android-test-maestro-cloud'))
    name: Generate and Test Test APK (Maestro Cloud)
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout project repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Setup Node + PNPM + install deps
        uses: ./.github/actions/setup-node-pnpm-install

      - name: 📦 Set Up JDK + Generate Test APK
        uses: ./.github/actions/setup-jdk-generate-apk
        with:
          APP_ENV: staging

      - name: Upload Test APK
        uses: actions/upload-artifact@v3
        with:
          name: test-apk
          path: ./android/app/build/outputs/apk/release/app-release.apk

      - name: 📱 Run E2E Tests with Maestro Cloud
        uses: mobile-dev-inc/action-maestro-cloud@v1.4.1
        with:
          api-key: ${{ secrets.MAESTRO_CLOUD_API_KEY }}
          app-file: ./android/app/build/outputs/apk/release/app-release.apk
          env: |
            APP_ID=com.obytes.staging
