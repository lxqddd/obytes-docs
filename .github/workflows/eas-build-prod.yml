# 🔗 链接：
# 源文件：https://github.com/obytes/react-native-template-obytes/blob/master/.github/workflows/eas-build-prod.yml
# Starter 发布流程：https://starter.obytes.com/ci-cd/app-releasing-process/

# ✍️ 描述：
# 此工作流用于为生产环境触发 EAS 上的构建。
# 可以从 actions 选项卡手动触发。
# 此工作流将使用 ./actions/eas-build 动作以生产环境触发 EAS 上的构建。

# 🚨 需要 GITHUB 密钥：
#         - EXPO_TOKEN：用于向 EAS 进行身份验证的 Expo 令牌
#         - 您可以从 https://expo.dev/settings/access-tokens 获取它

name: EAS Production Build (Android & IOS) (EAS)

on:
  workflow_dispatch:

jobs:
  Build:
    name: EAS Production Build (Android & IOS) (EAS)
    runs-on: ubuntu-latest
    steps:
      - name: Check for EXPO_TOKEN
        run: |
          if [ -z "${{ secrets.EXPO_TOKEN }}" ]; then
            echo "You must provide an EXPO_TOKEN secret linked to this project's Expo account in this repo's secrets. Learn more: https://docs.expo.dev/eas-update/github-actions"
            exit 1
          fi

      - name: 📦 Checkout project repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Setup Node + PNPM + install deps
        uses: ./.github/actions/setup-node-pnpm-install

      - name: ⏱️ EAS Build
        uses: ./.github/actions/eas-build
        with:
          APP_ENV: production
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
