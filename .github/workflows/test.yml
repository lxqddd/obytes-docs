# 🔗 链接：
# 源文件：https://github.com/obytes/react-native-template-obytes/blob/master/.github/workflows/test.yml

# ✍️ 描述：
# 此动作用于运行单元测试
# 在拉取请求和推送到 main/master 分支时运行
# 根据事件类型：
#   - 如果是拉取请求，它将运行测试并发布包含覆盖率详细信息的评论。
#   - 如果是推送到 main/master，它将运行测试并将检查添加到提交。

# 🚨 需要 GITHUB 密钥：无

name: Tests (jest)

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    name: Tests (jest)
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout project repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Setup Node + PNPM + install deps
        uses: ./.github/actions/setup-node-pnpm-install

      - name: 🏃‍♂️ Run Tests
        run: pnpm run test:ci

      - name: Jest Coverage Comment
        uses: MishaKav/jest-coverage-comment@main
        if: (success() || failure()) && github.event_name == 'pull_request'
        with:
          coverage-summary-path: ./coverage/coverage-summary.json
          summary-title: '💯 Test Coverage'
          badge-title: Coverage
          create-new-comment: false
          junitxml-title: 😎 Tests Results
          junitxml-path: ./coverage/jest-junit.xml
          coverage-title: 👀 Tests Details
          coverage-path: ./coverage/coverage.txt
          report-only-changed-files: true
