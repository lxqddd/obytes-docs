# 🔗 链接：
# 源文件：https://github.com/obytes/react-native-template-obytes/blob/master/.github/actions/setup-node-pnpm-install/action.yml
# 复合动作文档：https://docs.github.com/en/actions/creating-actions/creating-a-composite-action

# ✍️ Description:
# 这是一个复合动作，意味着它可以在其他动作中使用。
# 它几乎用于所有工作流来设置环境和安装依赖。
# 在这里更新包管理器或 Node 版本将反映在所有工作流中。

# 👀 使用示例：
#       - name : 📦 Setup Node + PNPM + install deps
#         uses: ./.github/actions/setup-node-pnpm-install

name: 'Setup  Node + PNPM + Install Dependencies'
description: 'Setup  Node + PNPM + Install Dependencies'
runs:
  using: 'composite'
  steps:
    - uses: pnpm/action-setup@v4
      with:
        run_install: false
    - uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: 'pnpm'

    - name: 📦 Install Project Dependencies
      run: pnpm install --frozen-lockfile
      shell: bash
